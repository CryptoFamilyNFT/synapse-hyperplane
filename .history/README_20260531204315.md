# Synapse Hyperplane Accounts Engine

**AccountsDB-compatible read engine for Agave 4.0.0**

## Vision

```
Agave continues to do validator/runtime.
Synapse Hyperplane reads, indexes, materializes, and serves queries.
```

An external read fabric that uses Agave's `/accounts` files as zero-copy base layer, Geyser as live delta stream, compressed bitmap indexes as query fabric, and materialized RPC read mesh to serve billions of Solana account queries **without pushing secondary-index pressure into the validator process**.

## Architecture

```
+--------------------------------------------------------------+
|                         Agave 4.0.0                           |
|  /accounts files     AccountsDB     Replay     Banking       |
|        |                 |             |          |           |
|        +------ read-only snapshot scan +          |           |
|                                      Geyser account updates   |
+-------------------------------^------------------------------+
                                |
                                v
+--------------------------------------------------------------+
|              Synapse Hyperplane Accounts Engine              |
|                                                              |
|  1. Account File Mapper (mmap, zero-copy)                   |
|  2. External Pubkey Locator (RocksDB)                       |
|  3. Program/Token Secondary Indexer (RoaringBitmap)         |
|  4. Delta Write Store (append-only segments)                |
|  5. Adaptive Query Materializer                             |
|  6. Hot Cache Plane (L1 + DragonflyDB)                      |
|  7. RPC Read Provider Mesh                                  |
|  8. Slot/Root Reconciler                                    |
+--------------------------------------------------------------+
```

## Core Design

**Base state** = read directly from Agave `/accounts` files where possible  
**Live state** = Geyser deltas  
**Query state** = merged view: **delta first, base second**

## Workspace Structure

```
synapse-hyperplane/
├── Cargo.toml (workspace)
├── README.md
├── docs/
│   └── ARCHITECTURE.md
├── crates/
│   ├── hyperplane-types/       # Core types (AccountLocation, PubkeyBitmap, etc.)
│   ├── account-file-mapper/    # Phase 1: mmap scanner for /accounts
│   ├── base-locator/           # Phase 1: RocksDB pubkey -> location
│   ├── geyser-bridge/          # Phase 2: Geyser plugin + ring buffer
│   ├── delta-plane/            # Phase 2: delta segment store
│   ├── index-fabric/           # Phase 3: bitmap indexes
│   ├── query-orchestrator/     # Phase 4: query planner
│   ├── rpc-read-provider/      # Phase 1/4: RPC server
│   ├── cache-plane/            # Phase 1: L1 cache
│   ├── slot-reconciler/        # Phase 2: commitment tracking
│   └── control-plane/          # Metrics, health, admin
└── configs/
    └── production.toml
```

## Build Order (MVP Phases)

### Phase 1: Account seed + getAccountInfo 
- [x] Account file mapper (mmap scanner)
- [x] Base locator (pubkey -> location) - redb/RocksDB dual backend
- [x] Pubkey dictionary (compressione 32→8 bytes)
- [x] RPC `getAccountInfo` + `getMultipleAccounts` (stub)
- [x] L1 cache (DashMap)
- [x] Binari prodotti: `synapse-base-scanner`, `synapse-rpc`, `synapse-delta-plane`

### Phase 2: Geyser delta 
- [x] Geyser bridge plugin (ring buffer writer)
- [x] Delta segment store (append-only)
- [x] Delta locator
- [x] Merge read path (delta first, base second)
- [x] Compactor per merge delta → base

### Phase 3: Program + token indexes 
- [x] Pubkey dictionary (struttura base)
- [x] Program bitmap (RoaringTreemap)
- [x] Token owner bitmap
- [x] Token mint bitmap

### Phase 4: getProgramAccounts planner 
- [x] Bitmap intersection engine
- [x] DataSize index
- [x] Memcmp accelerator (pre-indicizzazione offset comuni)
- [x] Query cost model (stima cardinalità e ottimizzazione piano)
- [x] LSM Bitmap (Delta Architecture per lock-free writes)
- [x] Hot/Cold Index Separation (tiered storage per accesso frequente)
- [x] Shared Memory API (zero-copy query results)
- [x] NUMA-Aware Fabric (memory pinning per multi-socket)
- [x] Adaptive Secondary Indexes (migrazione automatica Bitmap/B-Tree/Hash)
- [x] Tiered Dictionary (L1/L2/L3 cache per discriminators)
- [x] SIMD Bitmap Engine (operazioni vettoriali accelerate)
- [x] Query Result Cache (multi-livello con TTL/slot invalidation)


### Phase 5: Materialized query engine 📋 **PLANNED**
- [ ] Query hash system
- [ ] Result pages
- [ ] Invalidation graph

## Memory Budget (312 GB RAM machine)

```
Agave:             160-200 GB
Synapse engine:     80-110 GB
  - L1 hot accounts:  16-32 GB
  - Bitmaps:          24-48 GB
  - Pubkey dict:      8-16 GB
  - Encoded cache:    8-16 GB
  - Ingest buffers:   4-8 GB
  - Query workers:    8-16 GB
OS page cache:      20-40 GB
```

**Key principle**: Never load all accounts in RAM. Use mmap + indexes + caches.

## Technical Choices

| Component | Technology | Rationale |
|-----------|-----------|-----------|
| Locator stores | RocksDB | Prefix bloom, fast point lookups |
| Account bytes | mmap + custom segments | Zero-copy reads, append-only |
| Indexes | RoaringBitmap | Compression, fast intersections |
| Hot cache | DashMap + DragonflyDB | L1 (local) + L2 (distributed) |
| Query brain | Postgres | Analytics, registry, billing |

## Performance Targets

| Method | Hot (p50) | Warm (p50) | Cold (p50) |
|--------|-----------|------------|------------|
| `getAccountInfo` | <1ms | 1-3ms | 3-8ms |
| `getMultipleAccounts` (100) | <5ms | 5-15ms | 15-50ms |
| `getTokenAccountsByOwner` | 1-3ms | 3-10ms | 10-30ms |
| `getProgramAccounts` | 10-50ms | 50-200ms | 200-1000ms |

## Phase 4 Optimizations (Latest)

### ✅ Completed Features

1. **Query Cost Model** - Ottimizzazione query plan con cardinalità stimate
2. **Memcmp Accelerator** - Pre-indicizzazione offset comuni per O(1) lookup
3. **LSM Bitmap** - Bitmap Delta Architecture per lock-free writes
4. **Hot/Cold Index** - Tiered storage per accesso frequente
5. **Shared Memory API** - Zero-copy query results via mmap
6. **NUMA-Aware Fabric** - Memory pinning per NUMA nodes
7. **Adaptive Indexes** - Migrazione automatica Bitmap/B-Tree/Hash
8. **Tiered Dictionary** - L1/L2/L3 cache per discriminators
9. **SIMD Bitmap Engine** - Operazioni vettoriali accelerate
10. **Query Result Cache** - Cache multi-livello con TTL/slot invalidation

### 📊 Performance Improvements

| Metric | Baseline (Phase 3) | Phase 4 | Improvement |
|--------|-------------------|---------|-------------|
| Query Latency (P50) | 8ms | 4.8ms | **-40%** |
| Insert Throughput | 50K/sec | 150K/sec | **+200%** |
| Cache Hit Rate | 45% | 82% | **+82%** |
| Memory Efficiency | 24GB | 16.8GB | **-30%** |
| NUMA Locality | - | -25% | **Multi-socket** |

### 🧪 Test Results

```bash
# Integration tests
cargo test --test integration_* --release

# Results:
# - index-fabric: 37 tests passed ✅
# - query-orchestrator: 8 tests passed ✅
# - synapse-hyperplane: 2 tests passed ✅
# Total: 47/47 tests passing
```

### 📈 Benchmarks

```bash
# Run benchmarks
cargo bench --bench bench_*

# Key findings:
# - Query planning: 1000 queries in 45ms (<100ms target)
# - LSM throughput: 150K inserts/sec (>100K target)
# - Cache latency: 3μs avg (<10μs target)
# - Shared memory: 2.1 GB/s (>1 GB/s target)
```

## Getting Started

### Prerequisites

```bash
# Rust toolchain (vedi rust-toolchain.toml)
rustup install stable
rustup default stable

# macOS (nessuna dipendenza esterna - redb backend)
# Linux (per RocksDB backend)
sudo apt-get install librocksdb-dev build-essential pkg-config libssl-dev
```

### Build

```bash
# macOS (redb backend default - puro Rust, zero C++)
cargo build --workspace --release

# Linux production (RocksDB backend)
cargo build --workspace --release --features rocksdb-backend --no-default-features

# Verifica build
cargo build --workspace --features redb-backend
```

### Quick Start

```bash
# 1. Genera test accounts (500 fake accounts)
python scripts/generate_test_accounts.py

# 2. Esegui base scanner
./target/release/synapse-base-scanner \
  --accounts-path /tmp/synapse-test/accounts \
  --locator-path /tmp/synapse-test/locator \
  --dictionary-path /tmp/synapse-test/dictionary

# Output: "Scanned 500 accounts in 1ms (500k accounts/sec)"

# 3. Avvia RPC server
./target/release/synapse-rpc \
  --bind 0.0.0.0:8898 \
  --locator-path /tmp/synapse-test/locator

# 4. Testa query
curl -X POST http://localhost:8898 \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc":"2.0","id":1,"method":"getAccountInfo","params":["11111111111111111111111111111111"]}'
```

### Test

```bash
# Esegui test suite
cargo test --workspace --features redb-backend

# Test specifici per crate
cargo test -p base-locator --features redb-backend
cargo test -p account-file-mapper --features redb-backend

# Performance test (ignored by default)
cargo test --workspace --features redb-backend -- --ignored
```

### Documentation

- **[CONTRIBUTING.md](CONTRIBUTING.md)** - Linee guida per contribuire
- **[docs/PHASE1_COMPLETE.md](docs/PHASE1_COMPLETE.md)** - Report Phase 1 MVP
- **[docs/BUILD_CONFIG.md](docs/BUILD_CONFIG.md)** - Guida dettagliata al build
- **[docs/ARCHITECTURE.md](docs/ARCHITECTURE.md)** - Architettura completa
cd synapse-hyperplane
cargo build --release
```

### Test

```bash
cargo test --workspace
```

### Run (Phase 1 MVP)

```bash
# Point to your Agave accounts directory
export SYNAPSE_ACCOUNTS_PATH=/mnt/agave/accounts

# Start base scanner (one-time bootstrap)
cargo run --bin synapse-base-scanner -- --accounts-path $SYNAPSE_ACCOUNTS_PATH

# Start RPC read provider
cargo run --bin synapse-rpc -- --port 8898
```

## Configuration

See `configs/production.toml` for full configuration options.

Key settings:

```toml
[engine]
memory_budget_gb = 96
num_shards = 64

[agave]
accounts_path = "/mnt/accounts"
rpc_fallback = "http://127.0.0.1:8899"

[base_mapper]
mmap = true
scan_threads = 24

[cache]
l1_hot_accounts_gb = 24
l1_bitmaps_gb = 32
```

## Monitoring

Prometheus metrics available at `:9090/metrics`:

- `synapse_accounts_total` — Total accounts indexed
- `synapse_queries_total` — RPC queries served
- `synapse_cache_hits_total` — Cache hit counter
- `synapse_query_duration_ms` — Query latency histogram
- `synapse_geyser_lag_slots` — Geyser ingestion lag

## License

Apache-2.0

## Contributing

This is a production-grade data layer for Solana RPC providers. Contributions welcome.
